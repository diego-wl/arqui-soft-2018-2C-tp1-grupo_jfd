server { # simple reverse-proxy
    listen       80;
    #server_name  domain2.com www.domain2.com;
    #access_log   logs/domain2.access.log  main;

    location ~ ^/node(.*)$ {
        proxy_pass http://node:3000$1;
    }

    location ~ ^/py(.*)$ {
        proxy_pass http://python:8000$1;
    }
}
